import styled from 'styled-components';

export const Label = styled.label`color: ${(props) => props.theme.foreText};`;
export const Input = styled.input``;

export const Error = styled.p`color: ${(props) => props.theme.red};`;
