import React from 'react';
import { connect } from 'react-redux';
import { Link } from 'react-router-dom';

const JournalLink = (props) => {
	return <div>Journal Link</div>;
};

export default connect(null, {})(JournalLink);
